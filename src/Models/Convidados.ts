export class Contatos {
  nome: string
  email: string
  telefone: number

  constructor(nome: string, email: string, telefone: number) {
    this.nome = nome
    this.email = email
    this.telefone = telefone
  }
}
